```python
class ManufacturingQMLPipeline:
    def __init__(self, n_features=32):
        self.n_features = n_features
        self.scaler = StandardScaler()
        self.model = None

    def generate_synthetic_sensor_data(self, n_samples=1000, defect_rate=0.15):
        """製造ラインのセンサーデータを生成（デモ用）"""
        np.random.seed(2026)
        n_normal = int(n_samples * (1 - defect_rate))
        X_normal = np.random.randn(n_normal, self.n_features)
        n_defect = n_samples - n_normal
        X_defect = np.random.randn(n_defect, self.n_features) * 1.8 + 0.5
        # 非線形異常パターン追加
        mask = np.random.choice(self.n_features, size=8, replace=False)
        X_defect[:, mask] += np.sin(X_defect[:, mask] * 2) * 2
        X = np.vstack([X_normal, X_defect])
        y = np.hstack([np.zeros(n_normal, dtype=int), np.ones(n_defect, dtype=int)])
        idx = np.random.permutation(n_samples)
        return X[idx], y[idx]

    def train(self, X_train, y_train, X_val, y_val, epochs=50, batch_size=32, lr=0.001):
        X_tr = self.scaler.fit_transform(X_train)
        X_v = self.scaler.transform(X_val)
        X_tr_t = torch.FloatTensor(X_tr); y_tr_t = torch.LongTensor(y_train)
        X_v_t = torch.FloatTensor(X_v); y_v_t = torch.LongTensor(y_val)
        self.model = HybridQNN(n_features=self.n_features)
        optimizer = torch.optim.Adam(self.model.parameters(), lr=lr, weight_decay=1e-4)
        criterion = nn.CrossEntropyLoss(weight=torch.tensor([1.0, 5.0]))  # 不均衡対策
        scheduler = torch.optim.lr_scheduler.CosineAnnealingLR(optimizer, T_max=epochs)
        n_batches = len(X_tr_t) // batch_size
```
